name: brave-xenon-loris
description: A minimal Random Pulumi YAML program.
runtime: yaml
resources:
  username:
    type: random:RandomPet
    properties:
      length: 4
outputs:
  name: ${username.id}
config:
  pulumi:tags:
    value:
      pulumi:template: random-yaml
